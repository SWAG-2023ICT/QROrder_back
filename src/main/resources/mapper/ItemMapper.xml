<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="swag.qrorder.mapper.ItemMapper">
    <insert id="addItems" parameterType="List" useGeneratedKeys="true" keyProperty="itemId">
        INSERT INTO item (item_name, item_price, item_description, item_image, item_status, restaurant_id)
        VALUES
        <foreach collection="items" item="item" separator=",">
            (#{item.itemId},#{item.itemName},#{item.itemPrice},#{item.itemDescription},
                #{item.itemImage},#{item.itemStatus},#{item.restaurantId})
        </foreach>
    </insert>
</mapper>